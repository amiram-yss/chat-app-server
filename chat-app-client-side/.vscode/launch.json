{
    "configurations": [
        {
            "type": "pwa-chrome",
            "request": "launch",
            "name": "test",
            "url":"http://localhost:3000",
            "webRoot": "${workspaceFolder}"
        }
    ]
}